import { motion } from 'framer-motion';

export default function Header() {
  return (
    <motion.div className="relative h-64 overflow-hidden">
      {/* You can add animated header content here */}
    </motion.div>
  );
}
